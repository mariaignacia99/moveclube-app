services:
  - type: web
    name: moveclub-app
    env: python
    buildCommand: "pip install -r requirements.txt || true"
    startCommand: "python3 backend/server.py"
    plan: free
    autoDeploy: true
